# アプリケーション設定例

# === 基本設定 ===
# セッション/CSRF用のランダムキー
SECRET_KEY=replace_with_a_random_secret

# === DB設定 ===
# Cloud Run + Cloud SQL (MySQL/PyMySQL, Unixソケット)
# DATABASE_URL=mysql+pymysql://<USER>:<PASS>@/<DB_NAME>?unix_socket=/cloudsql/<INSTANCE_CONNECTION_NAME>
# ローカル SQLite
# DATABASE_URL=sqlite:///app.db

# DATABASE_URL を使わない場合は DB_* を指定
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=your_db_name
# INSTANCE_CONNECTION_NAME=your-gcp-project:region:instance
# TCP接続（Cloud Run以外やローカルMySQL）
# DB_HOST=127.0.0.1
# DB_PORT=3306
# Unixソケットを明示する場合
# DB_SOCKET=/cloudsql/your-gcp-project:region:instance

# === 環境種別 ===
# development / staging / production
APP_ENV=development
APP_DEBUG=true
# FLASK_DEBUG=1

# === チャット画像保存先 ===
# CHAT_ENABLED=true
# CHAT_IMAGE_STORAGE=local
# CHAT_IMAGE_DIR=chat_images
# CHAT_IMAGE_BUCKET=your-gcs-bucket

# === 生成画像保存先（未指定時はチャット設定を流用） ===
# GENERATION_IMAGE_STORAGE=local
# GENERATION_IMAGE_DIR=generated_images
# GENERATION_IMAGE_BUCKET=your-gcs-bucket

# === 初期ユーザー（必要時のみ） ===
# INITIAL_USER_USERNAME=admin
# INITIAL_USER_EMAIL=admin@example.com
# INITIAL_USER_PASSWORD=change-me

# === Vertex AI（推奨） ===
# Vertex AI を使う場合は必ず true
GOOGLE_GENAI_USE_VERTEXAI=true
GOOGLE_CLOUD_PROJECT=your-gcp-project
GOOGLE_CLOUD_LOCATION=us-central1

# === Gemini モデル指定（任意） ===
# GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
# GEMINI_TEXT_MODEL=gemini-3-pro-preview

# === Gemini API（APIキー方式） ===
# Vertex AI を使わない場合はこちらを設定（マスク画像を2枚目として送信する編集に対応）
# GEMINI_API_KEY=your_gemini_api_key_here

# === アップロード制限 ===
# MAX_IMAGE_WIDTH=8192
# MAX_IMAGE_HEIGHT=8192
# MAX_IMAGE_PIXELS=67108864
# MAX_CONTENT_LENGTH=33554432
# MAX_FORM_MEMORY_SIZE=33554432

# === ローカル用便利機能 ===
# DB_FORCE_SQLITE=true
# APP_AUTO_MIGRATE=true
# APP_AUTO_INIT_USER=true
